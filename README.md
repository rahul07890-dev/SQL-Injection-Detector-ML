# SQL-Injection-Detector-ML
A machine learning–powered SQL Injection Detection system using TF-IDF and Logistic Regression. Includes a modern Flask web interface, real-time prediction, interactive threshold tuning, and a custom SQL injection dataset.
